import { Eventdetailusecase } from "./evendetailusecase";

describe('Eventdetailusecase', () => {
  it('should create an instance', () => {
    expect(new Eventdetailusecase)).toBeTruthy();
  });
});
